# URL prefix for invocations of the EVent Streams schema registry's REST API
apicurio.registry.request.ssl.truststore.location=
apicurio.registry.request.ssl.truststore.password=
apicurio.registry.url=
# the below properties are not apicurio API related but sample related and serve to calculate both
# the sasl.jass.config and the apicurio.registry.request.headers.Authorization properties
event.stream.user=
event.stream.password=
